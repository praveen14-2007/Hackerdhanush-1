#include<stdio.h>
int main()
struct node
{
    int data;
    struct node* add;
};
struct node* head,*new node,*temp;
head=0,count=0,choice=0
while(choice==1)
{
    new code=(struct node*)malloc(size of(struct node));
    printf("enter data")
    
}